# Welcome to your prefect.yaml file! You can use this file for storing and managing
# configuration for deploying your flows. We recommend committing this file to source
# control along with your flow code.

# Generic metadata about this project
name: csv-api-dataflow
prefect-version: 3.0.0

# build section allows you to manage and build docker images
build:
  image: ehocalifornia/my-prefect-image:latest

# push section allows you to manage if and how this project is uploaded to remote locations
push:

# pull section allows you to provide instructions for cloning this project in remote locations
pull:
- prefect.deployments.steps.git_clone:
    repository: https://github.com/GrigoriyKruchinin/csv-api-dataflow.git
    branch: main
    access_token:

# the deployments section allows you to provide configuration for deploying flows
deployments:
- name: test_flow
  version:
  tags: []
  concurrency_limit: 1
  description:
  entrypoint: app/flows/data_flow.py:data_processing_flow
  parameters: {}
  work_pool:
    name: test
    work_queue_name: default
    job_variables:
      docker_image: ehocalifornia/my-prefect-image:latest
  enforce_parameter_schema: true
  schedules: []
